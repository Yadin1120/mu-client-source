// CrashReport.h — מלכודת קריסות לקליינט.
//
// עד עכשיו לקליינט לא היה שום מטפל בקריסות: חריגה לא מטופלת פשוט סגרה את
// התהליך, בלי קובץ, בלי שורה ב-MuError.log ובלי שום דבר לשאול עליו את השחקן.
// לכן גם קובץ סמלים (pdb) לבדו לא היה עוזר — לא היה מה לפענח.
//
// מה שנכתב כאן בקריסה:
//   1. שורות ב-MuError.log עם קוד החריגה, שם המודול וההיסט **בתוך המודול**.
//      ההיסט הוא מה שמאפשר לפענח לשם פונקציה מול ה-pdb של אותה בנייה בדיוק,
//      גם כשהכתובת המוחלטת שונה בכל הרצה בגלל ASLR.
//   2. קובץ minidump ליד המשחק, אם dbghelp זמין.
//
// ה-pdb **לא מופץ לשחקנים** — הוא נשמר בבית ליד הבינארי שפורסם, ומשמש לפענוח
// הדיווח שהשחקן שולח. אין טעם וגם אין רצון לחשוף אותו.

#pragma once

#ifdef _WIN32

namespace MuCrash
{
    /// <summary>
    /// מתקין את המטפל בקריסות. יש לקרוא פעם אחת, מוקדם ככל האפשר ב-WinMain.
    /// </summary>
    void Install();
}

#endif
